"""File pipeline exercise - build data processing pipelines with generators."""


class FilePipelineExercise:
    """Create efficient file processing pipelines using generators and decorators."""

    def __init__(self):
        self.name = "File Pipeline Exercise"
        self.description = "Build efficient data processing pipelines"

    def get_exercise(self):
        return {
            "title": "File Processing Pipeline",
            "description": "Create a pipeline that reads, processes, and writes data efficiently",
            "difficulty": "advanced",
        }


__all__ = ["FilePipelineExercise"]
